# 后端Docker环境变量配置

# 数据库配置（连接宝塔面板MySQL）
DB_HOST=localhost
DB_PORT=3306
DB_USER=monitor_user
DB_PASSWORD=BaotaUser2024!
DB_NAME=website_monitor

# Redis配置（可选）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Flask应用配置
SECRET_KEY=BaotaWebMonitorSecretKey2024
FLASK_ENV=production
LOG_LEVEL=INFO

# 应用配置
DOMAIN=localhost
BACKEND_PORT=15000

# 网络模式
# bridge: 默认桥接模式
# host: 使用宿主机网络（可直接访问宿主机MySQL）
NETWORK_MODE=host

# CORS允许的源地址
CORS_ORIGINS=http://localhost,http://127.0.0.1,https://your-domain.com

# 邮件配置（可选）
MAIL_SERVER=smtp.qq.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_DEFAULT_SENDER=